#ifndef _COMMANDE_
#define _COMMANDE_ 
#include "Item.h"


void talon(Cartes &it);

Cartes exposer(Cartes it);

Cartes poser(char mots);

void remplacer(Cartes &it, char mots);

void completer(Cartes it, char mots);

#endif // !_COMMANDE_

